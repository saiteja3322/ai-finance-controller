# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration (SQLite default for zero-dependency portability)
DATABASE_URL="file:./dev.db"

# JWT Authentication Secret
JWT_SECRET="ai-finance-controller-super-secret-jwt-key-2026"

# AI Configuration (Optional - fallback engine will be used if omitted)
GEMINI_API_KEY=""

# Razorpay Test Credentials (Optional - synthetic data fallback will be used if omitted)
RAZORPAY_KEY_ID=""
RAZORPAY_KEY_SECRET=""
